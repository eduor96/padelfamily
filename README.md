
  # Panel de reservas Padel

  This is a code bundle for Panel de reservas Padel. The original project is available at https://www.figma.com/design/phBcBTYxwXLpAxCujrCDkZ/Panel-de-reservas-Padel.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  